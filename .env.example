# Backend Database Configuration
# Use DATABASE_URL for Vercel Postgres / Neon / Heroku
DATABASE_URL=postgres://user:password@host:port/database

# OR use individual variables (fallback)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=webspk

# Environment
NODE_ENV=production

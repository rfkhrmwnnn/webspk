{
  "name": "webspk-root",
  "version": "1.0.0",
  "description": "Root for Vercel deployment",
  "main": "api/index.js",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "xlsx": "^0.18.5"
  }
}
